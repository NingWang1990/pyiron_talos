queue_type: SLURM
queue_primary: talos_cpu
queues:
  talos_cpu: {cores_max: 1200, cores_min: 1, run_time_max: 5760, script: talos_cpu.sh}
  talos_gpu: {core_max: 1200,  cores_min: 1, run_time_max: 5760, script: talos_gpu.sh}

